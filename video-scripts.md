# UNmemos 视频文案

---

## 方案一：30秒短视频（竖屏 · TikTok / Reels / 抖音）

### 标题建议
「你的闪念，不该碎在一堆 txt 文件里」

| 时间 | 画面 | 旁白 / 字幕 |
|------|------|-------------|
| 0-4s | 手机/平板快速打字："今天想到一个超棒的写作点子！" → 点击发送 → 出现在时间线上 | "记想法，不需要建文件、选文件夹、起文件名。" |
| 4-10s | 手指滑动时间线浏览卡片流，按天分组自动排列 | "随手一条，自动排进时间线——像朋友圈，但只属于你。" |
| 10-17s | 侧边栏标签树展开，拖拽排序，点击过滤 | "#灵感 #小说，标签自动提取。侧边栏一目了然。" |
| 17-23s | 切 Obsidian 白板：打开 `Memos/2025.canvas`，卡片以文本节点展示 | "数据存在 Canvas 文件里——直接在白板里看、拖拽、重新排列。" |
| 23-28s | 在任意笔记里输入 `@` → 弹出已有 memo 引用列表 → 点击插入 | "写笔记时输入 @，直接引用已有的 memo——跨文档联动零成本。" |
| 28-30s | LOGO + "完全离线 · 不联网 · 不收集任何数据" + GitHub 链接 | "完全离线，零遥测。试试 UNmemos。" |

### BGM 建议
轻快 acapella 或 lo-fi beat，节奏约 105 BPM。

### 封面文案
**"Obsidian 里的 Memos —— 闪念秒存"**

---

## 方案二：2分钟功能详解（横屏 · B站 / YouTube）

### 结构大纲

#### ① 开场（0:00-0:12）
- **画面**: 黑屏 → 键盘敲击声 → 一行文字浮现："我有一个新点子" → 点击发送 → 出现在时间线上
- **旁白**: "我们每天会有很多'等一下要记住'的瞬间。UNmemos 让这一刻变得干脆——打完字，发送，剩下的交给它。"

#### ② 核心理念：Canvas 原生存储（0:12-0:35）
- **画面**: 
  1. 左侧 UNmemos 视图右侧面板中快速记录多条内容
  2. 切换回 Obsidian 文件浏览器 → 打开 `Memos/2025.canvas`
  3. Canvas 视图中所有 memo 作为文本节点呈现
  4. 直接在画布上拖拽、连线、添加其他笔记嵌入
- **旁白**: "大部分速记工具把数据锁在自己的数据库里。UNmemos 不同——它的数据是原生的 Obsidian Canvas 文件，默认放在 Memos/{年份}.canvas。这意味着什么？意味着你可以直接用 Obsidian 的白板视图打开它们，随意拖拽、连线、嵌入其他笔记。你的 memo 不是孤岛，而是第一公民。"

#### ③ 闪念速记 & 时间线（0:35-1:00）
- **画面录屏顺序**:
  1. 顶部输入框打字（光标聚焦快捷键 `Cmd+Shift+N` 闪烁提示）
  2. 点击发送 → 卡片出现在时间线
  3. 快速连发 3-4 条不同类型的内容（纯文本、带标签、带 emoji）
  4. 草稿自动恢复演示（关闭再打开）
  5. 活跃度热力图滚动展示
- **旁白**: "顶部就是一个简洁的输入框——支持完整 Markdown，打标签用井号、加链接、贴图片都行。未发送的草稿会自动保留，关了就开，开了就续。按天分组的卡片流，加上活跃度热力图，让你一眼看到自己的思维脉搏。`Cmd+Shift+M` 打开面板，`Cmd+Shift+N` 直达输入框——零延迟启动。"

#### ④ 标签 & 过滤系统（1:00-1:20）
- **画面录屏顺序**:
  1. 侧边栏标签树：展开/收起多个标签层级
  2. 置顶一个标签，拖拽调整排序
  3. 点进某个标签 → 只显示相关 memo
  4. 日期范围过滤器 + 全文关键字搜索组合使用
  5. 保存为快捷过滤方式（flomo 风格）
- **旁白**: "正文中的井号标签自动提取进侧边栏。标签可以置顶、拖拽排序。内置全文、标签、日期组合过滤，还支持保存过滤条件为快捷方式——跟 flomo 用户一样熟悉的操作。"

#### ⑤ @提及 & 跨文档联动（1:20-1:40）
- **画面录屏顺序**:
  1. 在任何笔记（源码模式/实时预览/白板文本节点）中输入 `@`
  2. 弹出 memo 引用列表，打字筛选
  3. 选择一个插入 → 悬浮预览显示完整内容
  4. 切换到另一个文档，输入 `@@名字` 快速新建
  5. 反链计数显示引用数量
- **旁白**: "@提及是它的杀手锏。在任意编辑器输入 @——无论普通笔记、实时预览、还是白板文本节点——都能弹窗选择已有的 memo 来引用。引用后还能悬浮预览内容和反链计数。想新建？`@@名字` 一行搞定。"

#### ⑥ 隐私声明（1:40-1:50）
- **画面**: 黑色背景 + 白色文字逐行浮现
- **旁白**: "UNmemos 完全离线运行。不联网、不收集任何遥测数据。可选调试日志默认关闭且仅在内存中，由你主动导出。你的想法只属于你。"

#### ⑦ 结尾（1:50-2:00）
- **画面**: LOGO + 双语 UI 切换示意 + 安装指引 + GitHub 链接
- **旁白**: "UNmemos —— 双语界面、自定义主题色、移动端全覆盖。社区插件目录搜 UNmemos，或者去 GitHub 找到 UNcore-gh/UNmemos。试试吧。"

---

## 方案三：极简口播稿（直播/长视频插入片段，1分钟）

> "三个理由让你需要 UNmemos。
>
> 第一，它是 Canvas 原生的。别人的速记工具锁数据在自己数据库里；UNmemos 的数据就是 .canvas 文件——你随时可以用白板打开、拖拽、连线。你的数据永远是你的。
>
> 第二，@提及打通了 Obsidian 的全局。在笔记里输入 @，直接引用已有的闪念；反过来，memo 之间的互引也自动计算反链数。知识网络从此更密。
>
> 第三，它尊重你的隐私。零网络请求，零遥测。没有账号、没有云端、没有后台。
>
> 0BSD 开源。链接在描述区。"

---

## 素材需求清单

| 序号 | 素材类型 | 内容说明 |
|------|----------|----------|
| 1 | GIF/录屏 | 顶部输入框 → 快速发送 → 时间线卡片流出现（含草稿自动恢复） |
| 2 | GIF/录屏 | 侧边栏标签树操作：展开 → 置顶 → 拖拽排序 → 点击进入过滤 |
| 3 | GIF/录屏 | 活跃度热力图滚动展示 |
| 4 | 截图/GIF | Canvas 文件视图：`Memos/2025.canvas` 中卡片以文本节点形式呈现 |
| 5 | GIF/录屏 | 任意编辑器输入 `@` → 弹窗选择引用 → 悬浮预览 |
| 6 | GIF/录屏 | `@@名字` 快速新建 memo |
| 7 | 截图 | 过滤器界面：日期范围 + 全文搜索 + 快捷保存按钮 |
| 8 | 截图 | 设置页：自定义主题色 + 双语切换 |
| 9 | LOGO/品牌 | UNmemos 图标 + UNcore 品牌元素 |
| 10 | 截图 | `.screenshots/current_state.png`（已有可用） |
